
@{ Layout = "_LayoutAdmin"; }

@{
    ViewData["Title"] = "Admin";
}

<div>
    <div class="card">

        <div class="card-header">
            Informe 1
        </div>

        <div class="card-body">
            <form method="post" action="@Url.Action("GenerarInforme1", "Admin")">
                <div class="d-flex">

                    <div class="input-group col-6">
                        <span class="input-group-text" id="inputGroup-sizing-default">Fecha</span>
                        <input type="date" class="form-control" name="fecha" required>
                    </div>

                    <button type="submit" class="btn btn-primary col-6">Generar</button>
                </div>
            </form>
        </div>

    </div>

    <div class="card">

        <div class="card-header">
            Informe 2
        </div>

        <div class="card-body">
            <form method="post" action="@Url.Action("", "")">
                <div class="d-flex">

                    <div class="input-group col-6">
                        <span class="input-group-text" id="inputGroup-sizing-default">Fecha</span>
                        <input type="date" class="form-control" name="fecha" required>
                    </div>

                    <button type="submit" class="btn btn-primary col-6">Generar</button>

                </div>
            </form>
        </div>

    </div>

    <div class="card">

        <div class="card-header">
            Informe 1
        </div>

        <div class="card-body">
            <form method="post" action="@Url.Action("", "")">
                <div class="d-flex">

                    <div class="input-group col-6">
                        <span class="input-group-text" id="inputGroup-sizing-default">Fecha</span>
                        <input type="date" class="form-control" name="fecha" required>
                    </div>

                    <button type="submit" class="btn btn-primary col-6">Generar</button>

                </div>

            </form>
        </div>

    </div>

</div>
