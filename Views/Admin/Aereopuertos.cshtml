@model IEnumerable<Aereopuerto>

@{ Layout = "_LayoutAdmin"; }

@{
    ViewData["Title"] = "Admin";
}

<div class="text-center">
    <h1 class="display-4">Aereopuertos</h1>
    
    @*<form method="post" action="@Url.Action("GuardarAereopuerto", "Admin")">
        <div class="card" >
            <div class="card-body">

                <h5 class="card-title">Agregar aereopuerto</h5>

                <div class="card-body d-flex ">

                    <div class="input-group flex-nowrap col-6">
                        <span class="input-group-text" id="addon-wrapping">
                            <i class="bi bi-building"></i>
                        </span>
                        <input type="text" class="form-control" placeholder="Nombre" aria-label="nombre" name="nombre" aria-describedby="addon-wrapping" required>
                    </div>

                    <div class="input-group flex-nowrap col-5">
                        <span class="input-group-text" id="addon-wrapping">
                            <i class="bi bi-flag"></i>
                        </span>
                        <select class="form-select w-100" id="pais" name="pais" required>
                            <option selected>Pais...</option>
                            <option value="guatemala">Guatemala</option>
                            <option value="argentian">Argentina</option>
                            <option value="brasil">Brasil</option>
                        </select>
                    </div>

                    <div class="col-1">
                        <button class="btn btn-primary"> Guardar </button>
                    </div>

                </div>
            </div>
        </div>
    </form>*@

    <div class="mt-3">
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Nombre</th>
                    <th scope="col">Ciudad</th>
                    <th scope="col">Pais</th>
                    @*<th scope="col">Acciones</th>*@
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
	            {
                    <tr>
                        <th scope="row">@item.Id</th>
                        <td>@item.Nombre</td>
                        <td>@item.Ciudad</td>
                        <td>@item.Pais</td>
                        @*<td>
                            <span class="badge badge-success">Activo</span>
                        </td>
                        <td>
                            <a href="" class="btn btn-danger">
                                <i class="bi bi-dash-circle"></i> Deshabilitar
                            </a>
                        </td>*@
                    </tr>
	            }
            </tbody>
        </table>
    </div>
</div>
